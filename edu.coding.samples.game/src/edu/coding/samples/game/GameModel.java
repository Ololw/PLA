package edu.coding.samples.game;


public class GameModel {
	Board map;
	Game m_game;

	GameModel(Game game) {
		m_game = game;
		map = new Board(10);

	}
	
	/**
	 * Simulation step.
	 * 
	 * @param now
	 *            is the current time in milliseconds.
	 */

}
